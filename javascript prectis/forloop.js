// for loop
/*
let sum = 0;
let n = prompt("Enter the value of n");
n = Number.parseInt(n);

for (let i = 0; i < n; i++) {
  sum += i + 1;
}

console.log("Sum of first " + n + " Natural number is " + sum);
*/

// for in loop
/*
let obj = {
  Mango: 100,
  Banana: 30,
  Orange: 60,
};

for (let a in obj) {
  console.log("Prise of " + a + " are " + obj[a]);
}
*/

// for of loop

for (let obj of "Arpit") {
  console.log(obj);
}
